// This file contains configuration settings for your app

const config = {
    // Set this to true to use the self-hosted version of TinyMCE (no API key required)
    // Set to false to use the cloud-hosted version (requires API key)
    useSelfHostedTinyMCE: false,
    
    // If using cloud-hosted TinyMCE, enter your API key here
    // Get a free API key at https://www.tiny.cloud/auth/signup/
    tinyMCEApiKey: 'your-api-key-here'
};

export default config;
